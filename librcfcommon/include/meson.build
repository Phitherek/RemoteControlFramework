if build.name() == 'windows'
    install_headers('include\MHashEngine.h', 'include\librcfcommon.h', subdir : 'librcfcommon')
else
    install_headers('include/MHashEngine.h', 'include/librcfcommon.h', subdir : 'librcfcommon')
endif
